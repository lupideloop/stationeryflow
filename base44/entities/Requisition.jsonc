{
  "name": "Requisition",
  "type": "object",
  "properties": {
    "request_date": {
      "type": "string",
      "format": "date"
    },
    "requester_name": {
      "type": "string"
    },
    "department": {
      "type": "string"
    },
    "owner_user_id": {
      "type": "string",
      "description": "The stock owner this requisition belongs to"
    },
    "status": {
      "type": "string",
      "enum": [
        "Pending",
        "Reviewed"
      ],
      "default": "Pending"
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "item_name": {
            "type": "string"
          },
          "quantity": {
            "type": "number"
          },
          "matched_item_id": {
            "type": "string"
          },
          "stock_level": {
            "type": "number"
          }
        }
      }
    }
  },
  "required": [
    "request_date",
    "requester_name",
    "department"
  ],
  "rls": {
    "read": {
      "data.owner_user_id": "{{user.id}}"
    },
    "create": {
      "data.owner_user_id": "{{user.id}}"
    },
    "update": {
      "data.owner_user_id": "{{user.id}}"
    },
    "delete": {
      "data.owner_user_id": "{{user.id}}"
    }
  }
}